# GENERATED by docker/generate-deps.R - do not edit by hand.
# R 4.4.3 | PPM snapshot 2026-06-01 | platform ubuntu-24.04
# Top-level packages: rmarkdown, readxl, knitr, tinytex, kableExtra, RColorBrewer, RCurl, jsonlite, tidyverse, stringr, treemap, maditr, here, extrafont, float, systemfonts, svglite
#
# Apt packages for the full dependency closure (one per line).
# Consume in Dockerfile.base with e.g.:
#   grep -vE '^\s*#|^\s*$' docker/sysreqs.txt | xargs apt-get install -y --no-install-recommends
#
cmake
libcurl4-openssl-dev
libfontconfig1-dev
libfreetype6-dev
libfribidi-dev
libglpk-dev
libharfbuzz-dev
libicu-dev
libjpeg-dev
libpng-dev
libssl-dev
libtiff-dev
libuv1-dev
libwebp-dev
libx11-dev
libxml2-dev
make
pandoc
zlib1g-dev

# --- reference: pak install_scripts ---
# apt-get -y install cmake make libuv1-dev pandoc libicu-dev libfreetype6-dev libfribidi-dev libharfbuzz-dev libxml2-dev libcurl4-openssl-dev libx11-dev libssl-dev zlib1g-dev libjpeg-dev libpng-dev libtiff-dev libwebp-dev libglpk-dev libfontconfig1-dev
